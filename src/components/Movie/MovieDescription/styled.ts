import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles({
    table: {
        minWidth: "450px",
        display: "flex",
    },
    tableBody: {
        width: "100%",
    },
});

export default useStyles;
